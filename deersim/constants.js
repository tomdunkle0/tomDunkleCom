/*--------------------------------------------------*\
 | File: deersim/constants.js                       |
 | Purpose: Defines constant values used in the     |
 | game logic of I-84 Simulator for "the Internet". |
\*--------------------------------------------------*/

const CONST_BAR_WIDTH          = 150;   // Standard width of a Bar object.
const CONST_BAR_HEIGHT         = 16;    // Standard height of a Bar object.
const CONST_CANVAS_HEIGHT      = 720;   // The height of the game display.
const CONST_CANVAS_WIDTH       = 1280;  // The width of the game display.
const CONST_DEER_WIDTH         = 55;    // The visible width of a Deer.
const CONST_DIM_MEDIUM         = 64;    // Standardized image dimension.
const CONST_DIM_LARGE          = 128;   // tandardized image dimension.
const CONST_DIM_XLARGE         = 256;   // Standardized image dimension.
const CONST_ENVIRONMENT_I84    = "i84"; // Environment handle for the I-84 level.
const CONST_FALSE              = 0;     // Used to store the boolean value false.
const CONST_FONT_SIZE_LARGE    = 32;    // The dimension in pixels of large text.
const CONST_FONT_SIZE_SMALL    = 16;    // The dimension in pixels of small text.
const CONST_FONT_SIZE_TINY     = 8;     // The dimension in pixels of tiny text.
const CONST_LANE_0_BASE        = 630;   // Y position for objects found below lanes.
const CONST_LANE_0_INDEX       = 0;     // Index representing lane #0.
const CONST_LANE_1_INDEX       = 1;     // Index representing lane #1.
const CONST_LANE_2_INDEX       = 2;     // Index representing lane #2.
const CONST_LANE_3_INDEX       = 3;     // Index representing lane #3.
const CONST_LANE_4_INDEX       = 4;     // Index representing lane #4.
const CONST_LANE_HEIGHT        = 40;    // The width in pixels of the road's lanes.
const CONST_LINE_HEIGHT_SMALL  = 24;    // The height of a line of small text.
const CONST_MS_PER_SEC         = 1000;  // Milliseconds per second.
const CONST_POWERUP_LEN_FRAMES = 600;   // Standard time duration of a powerup.
const CONST_MAX_REMAINING_DEER = 20;    // The maximum number of remaining Deer for a Profile.
const CONST_SIZE_SMALL         = 16;    // Standardized text/object size.
const CONST_TRUE               = 1;     // Used to store the boolean value true.
